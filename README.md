# Node API Starter (TypeScript)

Quick start:

1. Copy `.env.example` to `.env` and tweak if needed.
2. `npm install`
3. `npm run dev`

This is an industry-style starter (Controller → Service → Repo), in-memory DB for demo.
